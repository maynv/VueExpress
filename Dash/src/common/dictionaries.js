export const dictionaries = {
    messages: {
        // common messages
        required: () => "error_message_common_require"
    },
    custom: {
        "input-validator": {
            required: () => "error_message_common_require"
        }
    }
};
